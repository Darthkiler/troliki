<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.darthkiler.troliki">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/qwe"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity
            android:name=".login"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".menu"
             />
        <activity android:name=".choice_ruta" />
        <activity android:name=".registration" />
        <activity android:name=".choice_statie" />
        <activity android:name=".write_orar" />
        <activity android:name=".choice_second_statie" />
        <activity android:name=".timp_intre_statii" />
        <activity android:name=".choice_timp" />
        <activity android:name=".timp_de_asteptare" />
        <activity android:name=".write_ruta" />
    </application>

</manifest>